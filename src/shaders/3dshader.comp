#version 450

layout(set = 0, binding = 0) uniform UniformbufferObject {
    mat4 mvp;
} ubo;

layout(location = 0) in vec3 position;
layout(loction = 0) out vec3 fragPos;

void main() {
    fragPos = position;
    gl_Position = ubo.mvp * vec4(position, 1.0);
}
